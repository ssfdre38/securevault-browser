# Build outputs (too large for git)
builds/*.tar.gz
builds/*.deb
builds/*.zip
builds/*.msi
builds/*.AppImage
builds/*.rpm

# Keep checksums and metadata
!builds/*.sha256
!builds/*.sha512
!builds/*.verify
!builds/*.list
!builds/*.json
!builds/*.html
!builds/*.md
